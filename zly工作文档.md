# 2026-2-16
深势科技那个其实也是可以的，我感觉就是我们两个之间的协作还是有问题，就是我这边没有定非常清晰的协作模式，就是协作的时候确实还是需要就是有一个人主导的，这也就是领导的职责。

## 我现在想通过定义用户的流线的数据集去驱动claude code写代码

### 当前的产品
作为产品经理，我将该项目定义为一款针对生化环材领域的“科研全链路闭环引擎”。其核心功能应聚焦于打破“阅读-实验-写作”的信息断层，通过原子化笔记与流线式约束实现科研资产的数字化复盘。

具体功能总结如下：

1. 原子化对象管理系统
构建以 **Concept（主题）**为核心的六类标准笔记模型。

Ingest 阶段： 实现文献（Paper）到**可证伪断言（Claim）**的转化，强制要求挂载 Evidence（原文证据），确保结论可溯源。

Plan 阶段： 将构想（Insight）转化为具备 **Due_date（截止时间）**的任务，并自动化生成结构化 Protocol（实验 SOP），解决计划落地难问题。

Close 阶段： 记录实验执行，强制关联 raw_data_paths（数据路径），产出 Verdict（支持/反驳判定）。

2. 强约束流线（Workflow Enforcement）
通过 Web 端 UI 设定“准入准出”规则：

无证据不存 Claim：从源头过滤模糊认知。

无时间不设 Plan：防止实验无限期拖延。

无路径不结 Loop：确保每一条论文结论都能瞬间调取底层的原始实验数据。

3. “双端共生”数据架构
规范层（Web UI）：负责复杂的逻辑校验与图谱可视化。

资产层（Local Markdown）：所有数据以标准 MD 格式落盘至本地 Vault，实现与 Obsidian/VS Code 的无缝兼容，确保科研人员的数据主权与长期安全性。

核心价值： 该项目不只是记录工具，而是通过流程约束，将碎片化的实验动作转化为**“证据-执行-结果”的审计链路，并自动沉淀为写作素材**。

### 需要做的事情
上面的项目仅代表我的想法，并没有实现，只实现了一个前端。

我现在要做的是需要你从我上传我真实的科研文档yyq_obsidian ZIP 中构造一个全生命周期的测试样例。我现在要做的是利用亚氯酸盐讲解污染物，并且输出一篇论文。我的全部科研文档都放在这个yyq_obsidian文件夹里面。
我希望构造一个全生命周期的测试样例的样子是这样的，比如用户每天的对话记录和上传的实验数据记录，这里相当于模仿了用户是如何从零一步步的进行科研工作的。
最后交付给我一个ZIP的文件，有下面的内容
一个全生命周期的测试样例应该是包含
1、一系列全生命周期测试样例的pydantic模型定义，是py文件，很明显这里需要有上传文件的实际模拟，文件可以是 图片或者csv，或者用户的提问prompt，这些都需要有对应的模型定义来模拟用户的输入和系统的输出。
2、一个全生命周期测试样例的具体json案例，你也顺便模拟系统的结果，也就是生成一个真值。
3、其他配套的png文件和csv 这些从我上传的科研文档yyq_obsidian ZIP中来，这个要和json样例的对话路径对应起来，PNG文件可以是上传的科研文档里面已经有的ZIP文件，csv文件可以是一些从markdown里面我记录的数据文件
4、不少于150轮的提问交互，按照时间顺序，并且每一轮的交互一定要从我上传的yyq_obsidian ZIP中来，而不是在自己想象，是模拟用户真实提出的问题或者上传的数据，或者困惑这些的。最后能够完成这个论文.一定不要少于150轮交互，并且每次模拟的用户提问都不少于40个字，不要生成无意义的，不推进实验或者论文书写的问题不用假设。



深入思考80分钟之后给我答案

# 2026-2-17
我是觉得构建评测集的本身也是在想产品的过程。
感觉未来是通用持久化agent的设计，而且是通用agent的设计。而通用agent是没什么关系的，主要就是上下文的构建问题和workspace的构建问题，skill有外挂的skill。tool就是通用tool就好
最后设计的时候肯定上传是API,只不过这个整前端的请求本质上都是在看后端的workspace有哪些东西。
这样就是可以吧一部分的信息外包给产品，产品需要做的就是构建agent全生命周期的评测数据集，然后还有就是书写skill和workspace的方案，然后都是比较通用智能体的逻辑。
2、回答的时候假定作者可以调用GPT5PRO制作PPT，codex，提示咸鱼购买账号，这两个门槛低，但是要直接给出压缩的路径，也就是需要把对应的文件copy过去

# workspace
长期目标.md
## 
到XXX的目标.md
当前子实验进展管理文件.md
相关文献支撑管理.md

##
现在的问题是这个json实在是有点不太像人写的，就是这里实在是需要更多的人类化的表达和细节。
1、用户的text不应该这样写，应该是类似于真实的提问，因为给你的是笔记的软件，所以这个里面的文本更多是笔记的文本，但是你要根据这个笔记的文本反推下我记这个笔记的时候应该有问题，或者我想做什么，也就是我的笔记更多应该是答案的参考，你需要口气什么的要模拟一个真实用户的提问。
2、也不用分stage尽量只有这个最简洁的信息，stage现在不区分，之前的产品设计也没有太想清楚。
3、现阶段不用想回复是还是什么，只用模拟用户的输入，关键就是这里用户的输入一定要模拟好
4、不用source，保持数据结构的简单性，重点是这个交互前后的连续性和相关性，要从我给你的文档里面来。
所以生成的json是下面的样子，这里至少应该有150次对话，差不多90天，你在反复阅读之前上传的zip文件之后，深入思考80分钟给我新的ZIP文件
{
  "scenario_id": "yyq_chlorite_full_lifecycle_v1",
  "generated_at": "2026-02-16T12:00:00+08:00",
  "conversation_with_source": [
    {
      "turn_id": "T0001",
      "timestamp": "2025-01-01T09:00:00+08:00",
      "user": {
        "text": "# 战略\n背景部分信息太多，不够聚焦\n开题主要是交代一个漏斗一样的背景，为什么是亚氯酸盐\n- 不能输因为没人做就用它\n\t- 常见的优势是\n\t\t- 不需要加其他外来氧化剂\n\t\t- 对于难降解去除污染物优势\n\t-所以你要研究它\n\t然后你打算通过什么方式研究它？你这条路径为何j就可行",
        "uploads": []
      }
    },
    {
      "turn_id": "T0038",
      "timestamp": "2025-09-11T09:16:00+08:00",
      "user": {
        "text": "不应该这样写，应该是类似于真实的提问：#日志20250919 \n师兄说效果好，考虑降低实验浓度，0.1mM到0.5mM逐步增加0.1mM（母液NaClO2的量减半）。\n![[b3f06cf1277befa2d7e065964d3fcc7f.png]]",
        "uploads": [
          {
            "path": "assets/png/b3f06cf1277befa2d7e065964d3fcc7f.png",
            "media_type": "image/png",
            "role": "raw_data",
            "source_vault_path": "b3f06cf1277befa2d7e065964d3fcc7f.png"
          },
          {
            "path": "assets/csv/table_03_第二阶段_材料合成方法汇总.csv",
            "media_type": "text/csv",
            "role": "raw_data",
            "source_vault_path": "1-主逻辑树-自上而下（脉络）+自下而上（结果）/2-材料合成方法/【第二阶段】材料合成方法汇总.md"
          }
        ]
      }
    }
  ]
}

# 2026-2-19
## 最新版的给GPT5PRO的提示词
这里面是我根据你上传的ZIP构建的全生命周期测试样例。你看下是否符合你的预期，就是感觉下和你提问是不是一样，我是觉得后期可以引入下面的事情，你要不看下最新的提示词要如何书写。感觉要花30分钟来好好的打磨提示词让它重新生成评测的集合。

### 再修改一轮的提示词

模拟天数拉长到180天，这样更符合一篇化学or材料类论文的书写时间，除了写论文以外，这中间还可以穿插每隔一周or两周or三周不定期给导师汇报的任务，需要制作汇报的PPT，也就是这里需要有10次给老师汇报的提问，这些提问尽量是各不相同的，尽量需要有修改，但是主要是需要准备组会PPT。回答的时候假定作者可以调用GPT5PRO制作PPT，这里的提问模版是，但是这个仅仅是示例模板，你需要这X周的任务进行一个变化的提问
'''我明天要给老师汇报了，你帮我总结下从上次汇报以来的进展，尽量稍微丰富一些，然后帮我准备一个给wpsAI/扣子空间的提示词，要非常详细，不少于1000个字，然后把这些天的成果复制到指定的文件夹，我一口气上传给wpsAI/扣子空间让它制作PPT'''
1、时间拉长到180天之后，总的提问长度需要变化到300轮交互，最后期望的最终目标是能够写完一篇论文，其实你现在给出的提问已经非常像真人的提问了，但是却少了逻辑性和前后的连贯性.
1、在刚开始的时候主要是思路讨论和论文调研为主，也就是假设这里总共是180天的模拟，在最开始的30天主要是思路讨论和论文调研为主的问题，也可以引入实验设计，创新点查找这类问题。这里假设作者可以外部调用openai的deepresearch 而不是直接向你提问，一般用户是要求输出一个给deepresearch调研的提示词，以及上传了openai deepresearch结果然后让你保存成笔记的提示词。或者用户上传了PDF，用户可能会这样提问'''帮我总结下我上传的PDF文件，提取其中的核心点写到你的记忆里面，帮我思考下如何和当前的论文有什么的关系'''

2、中前期也就是（30天到70天的时候）可以逐步引入 实验攻坚与性能优化的 实验设计和数据收集的相关问题，主要解决从“是什么”上升到“为什么”，这是顶刊的门槛。包括不限于样品制备与优化，核心性能测试，核心机理最小闭环的验证（比如淬灭实验、活性物种如二氧化氯、高价钴氧物种的测试）这中间可能和AI产生的对话，比如【与我实验主题最相关的顶刊中对于这些实验的基础配置和详细的实验流程是怎样的，这些相同基础实验的结果范围是多少，我的与之相比合理与否或是好是坏】。但是这里要和我给你的ZIP文件的提示词里面的做实验的顺序是一致的，也就是需要需要参考ZIP的实验顺序来生成实验。在这期间也要插入使用py读取csv然后绘图的问题，就是比如用户提问'''我想根据我上传的实验数据然后进行绘图，你帮我用py使用seaborn调用绘图工具进行绘图。'''

3、中后期也就是（70天到140天的时候）逐步引入 深入表征与机理挖掘的包括不限于样品制备与优化，常规表征，理论计算这中间可能和AI产生的对话，比如材料形貌（xrd、tem、sem 等）、材料物理化学性质（raman、ftir、xps、同步辐射）、材料计算（pdos 图、吸附反应能垒）。但是这里要和我给你的ZIP文件的提示词里面的做实验的顺序是一致的，也就是需要需要参考ZIP的实验顺序来生成实验。这里面显然要模拟很多反复的情况，比如中后期的表征不能很高的支持中前期的结论，然后返回去重新做一些实验或者表征，然后要么就是也有可能之前的调研有问题需要重新调研。

4、绘图期（140天到150天）可以逐步引入实验结果的分析与讨论的内容，包括不限于数据处理与可视化，结果的可靠性与有效性分析，这中间可能和AI产生的对话，比如这里需要你帮助把实验结果按照常规论文的章节安排，从一句话提炼出实验可能的创新点作为主要的核心框架，并且按照（材料基础表征与形貌-性能测试+反应核心活性物种测试+基础机理测试-反应路径和材料计算-应用/毒理/稳定性的测试）帮我串联实验结果的图片，并且每个图片都要说清楚数据趋势+一句话结论+可能风险点并给出之前 1、2、3 阶段相应的证据。但是这里要和我给你的ZIP文件的提示词里面的做实验的顺序是一致的，也就是需要需要参考ZIP的实验顺序来生成实验。期间在实验结果的整合中很可能会验证出对于创新点的新的想法，但要综合考虑现有资源和截止日期判断是否要更新创新点，因为这可能涉及主脉络的修改程度大小。
你还需要模拟下面的问题
（1）根据这几个 xrd 的 raw 格式的 txt 数据实验效果和之前推测得到的 co3o4 可能形貌的结论，我需要制作论文的实验图，帮我模仿这片顶刊的构图与配色（要求标样pdf 卡片和实验样品的 pdf 卡片要对齐，且要标出对应晶面）生成一个详细的符合nature 顶刊的制图标准的nano banana的生图提示词
（2）这些实验结果属于【性能测试+反应核心活性物种测试+基础机理测试】，请你帮我凑出一条组会汇报的主线，要求每一页不超过 2 张图和 2 句结论并隔1-2 页提出一个可以向导师提问的核心问题
（3）这是材料表征的结果图片，请你根据我之前给你的库里的文献（如果证据不足就使用 deepresearch），你判断一下这些材料表征的核心结论对证明我的【性能测试+反应核心活性物种测试+基础机理测试】是否有利，输出必须带有这些表征结果图片中的核心结论以及它们如何证明我的【性能测试+反应核心活性物种测试+基础机理测试】这一部分的实验结果。
5、论文书写期（150天到180天）完成以下的内容Abstract & Introduction： 
这两部分主要基于用户在中前期的调研的文献生成 introduction，提炼出 gap 对应到创新点，简要介绍实验主脉络。
Results & Discussion： 基本按照第四阶段绘图期确定的章节结构（这不一定是章节最终名字，只是抽象一个大致的框架名）：材料基础表征与形貌-性能测试+反应核心活性物种测试+基础机理测试-反应路径和材料计算-应用/毒理/稳定性的测试
润色与格式检查： 检查语法，确保符合目标期刊的格式要求（References, SI 附件等）。
这段在我给你的ZIP里面都有一些关于开题报告的内容但并没有涉及到最终的论文写作，开题报告主要有我调研的文献、我搜集的别人的开题的架构（本质上是在对齐与其他人的相比我的主逻辑是否有问题），你可以基于这其中我的工作流发挥想象力模拟提问。

其他要求：
1、300轮的提问中有至少 30%需要有文件的上传，来模拟各种实验数据的处理，或者图片的提问，或者PDF的上传，PDF论文的上传你不用提供真实的PDF，但是需要模拟一个文件路径就好
请反复查看我之前上传给你的ZIP，然后深入思考80分钟之后再给我生成新的ZIP文件，还是需要有一个核心的300轮提问的json文件 和 asset文件夹在这个ZIP文件下面。

# 2026-2-22
后期的tool其实是可以加入浏览器操作的，还有canvas的功能的，但是现在感觉工具加入这些还比较复杂。现在就是要书写一个提示词然后就可以开始执行。
## 分裂PRD的提示词
帮我把根据现在的PRD 文档制作一份10轮的开发计划，主要是拆分实现的步骤，确保每一步都清晰可行，我会按照前后顺序让claude code 生成代码，并且要增加测试的写入。最后给我10个markdown文档。
感觉提示词文档可以直接给它openclaw的模板，然后让他具体的生成模板，而不是提示词，这样清晰许多。

# 2026-2-25
记忆系统还是要回去看openclaw的设计是最完备的。

yyq-改 cc 提示词问 gpt
我想设计一个模仿 openclaw 的 贯穿一个环境学生三个月学习周期的Researchloop-OpenClaw
•
## 我的目标
以下是我想学习的 openclaw 的设计原则
- 文件即记忆 (File-first Memory)：摒弃不透明的向量数据库，回归最原始、最通用的 Markdown/JSON 文件系统。用户的每一次对话、Agent 的每一次反思，都以人类可读的文件形式存在。—对应我的原子笔记系统
-	技能即插件 (Skills as Plugins)：遵循 Anthropic 的 Agent Skills 范式，通过文件夹结构管理能力，实现“拖入即用”的技能扩展。
- 透明可控：所有的 System Prompt 拼接逻辑、工具调用过程、记忆读写操作对开发者完全透明，拒绝“黑盒”Agent。
由此可以得到
在一个完整的180 天的实验周期中（如 json 所示），用户可以清晰的看到workspace（前端）中Researchloop-OpenClaw是如何拼接上下文（用户问题+agents.md、soul.md、user.md、+tools 和 skill 清单+专属于实验学科定制的记忆系统（如下原子笔记系统）），并引导用户输入相应的实验结果/表征结果/相关的论文，然后清晰地在前端的中间的 chatbot 中看见 agent 的思考过程（其中推理过程+每一步引用了哪个 md 文件+信息不足时需要用户提供什么 context），并将最后的交付结果存档。如果检测到高度重复的存档，则可以沉淀为有用户个人风格的新的 tools 和 skills

## 你的参考
### json文件
json 文件是我模仿用户在workspace 前端中整个周期的提问和输入。
### 我给的示例原子笔记系统（只做参考，根据你自己的定义来思考参考程度）
1.2 核心痛点：科研过程中的数据/信息断层
传统科研流程中，“阅读、计划、执行、复盘”往往分散在不同的任务中，导致数据产生与逻辑验证脱节。本系统将断层重组为六类原子笔记（Paper / Concept / Claim / Insight / Protocol / RunResult），并通过三条功能流线（Ingest / Plan / Close）串回一条可追溯和可验证的链路：
1. 证据难追溯 → 用 Claim（含 evidence）固化证据锚点
 阅读时产生的结论常只记成一句话，事后很难追到原文条件与上下文。系统在 Ingest 阶段把结论固化为 Claim，并要求同时记录evidence 证据摘录（来自 Paper）。没有 evidence 的 Claim 不允许写入知识库，从源头减少“凭印象做假设”。
- 原子笔记：Paper / Claim（在 Concept 下组织）
- 功能流线：Ingest
2. 计划难落地 → 用 Insight（含 due_date）把想法变成今日任务
 实验计划常口头化或碎片化，导致执行时偏离要验证的指标。系统在 Plan 阶段要求 Insight 必须包含 due_date，让“验证想法”天然具备时间约束，避免无限拖延。
- 原子笔记：Insight
- 功能流线：Plan
3. SOP 难复现 → 用 Protocol 结构化步骤与检查点
 实验过程依赖个人经验与临场记忆，复现成本高。系统将 Plan 输出固化为 Protocol（SOP）：步骤化、带检查点与偏差记录提示，降低复现实验的沟通成本。
- 原子笔记：Protocol
- 功能流线：Plan
4. 结果难回链 → 用 RunResult建立反向索引并沉淀论文写作素材
 原始数据常会储存在仪器设备或电脑文件夹中，写论文时难以快速找到“哪个结果支撑哪个结论”。系统在 Close 阶段要求 RunResult 必须包含 raw_data_paths（原始数据路径清单）。同时产出 Verdict（support / not-support / uncertain） 与写作草稿（Methods/Results），形成“数据—结论—写作素材”的双链。
- 原子笔记：RunResult
- 功能流线：Close
5. 工具信任与数据主权 → 用本地 Markdown Vault 落盘
 系统不引入云端数据库。所有核心对象均以 Markdown（含 YAML 元数据）落盘到本地 Vault，用户可用 Obsidian/VS Code 直接打开、编辑与检索，避免厂商锁定与数据外流顾虑。
以上对象在图谱中以双链形式可视化：
Concept → Paper/Claim → Insight → Protocol → RunResult，实现“证据—执行—结果”的全链路审计与复盘。
1.3 产品使命
构建“Web 端严格闭环”与“本地端自由掌控”的统一工作流。
 将科研人员的一天转化为可执行、可验证、可追溯的最小闭环：Web 端负责流程与规范，本地端负责长期资产与数据主权。

---
二、核心功能与 Demo 演示
[Image]
2.1 三件套定义：Concept / Claim / Insight
一句话记忆：Concept 定范围-大致的主题和边界，Claim 定断言，Insight 定今天怎么验证。
 它们分别对应 UI 顶部链条中的：Concept → Claim → Insight → Protocol → RunResult
Concept（今日验证对象）
- 是什么：一个“验证主题与边界”的容器，用来声明你这段时间研究什么。这个Concept是最后要书写论文的主题和关键词
- 何时创建：开始读文献之前，先选定或新建一个 Concept（比如“pH 窗口是否决定催化活性”）。
- 必须包含：name（主题名）+（可选）scope/keywords（边界与关键词）。
- 和谁相连：一个 Concept 下会聚合多个 Paper 和多条 Claim，是整条链路的起点与筛选器。
Claim（可证伪结论）
- 是什么：从 Paper 中提炼出来的、可以被实验支持或推翻的一句话断言。它不是笔记感想，而是“可验证的命题”。这个Claim是最后要书写论文的核心论点
- 何时创建：Ingest 阶段由系统从 Paper 生成（Demo 可用 mock）。
- 必须包含：claim_text（claim内容）+ evidence（原文证据摘录）。
- 和谁相连：每条 Claim 必须回链到对应 Paper；后续一个或多个 Insight 会选择某条 Claim 作为验证对象。
判别标准（用户自检）：如果这句话做完实验也无法判断对错，它就不是 Claim。
Insight（验证想法）
- 是什么：针对某条 Claim 的“今天要怎么验证”的具体设想，它把抽象断言转成可执行任务。
- 何时创建：Plan 阶段由用户在选择 Claim 后填写。
- 必须包含：insight_text（验证想法）+ due_date（截止日期）。
- 和谁相连：Insight 必须指向一条 Claim；系统据此生成 Protocol（实验SOP，实验的具体操作步骤） 与后续 RunResult 记录模板。
判别标准（用户自检）：Insight 至少要回答一个问题：我准备改哪个变量/观察哪个指标来判断 Claim？
2.2 极简闭环工作流（3 Click Loop）
为降低认知负荷，系统 UI 只保留三个核心阶段，并通过硬性校验规则确保闭环质量
1. Ingest：Paper → Claim（含 evidence，这里evidence可以具体追溯到上传的paper的具体的链接，类似于秘塔科技的点击跳转）
 用户在某个 Concept 下导入文献。系统生成 Paper.md 与若干 Claim.md；每条 Claim 必须包含 evidence 摘录，否则不允许写入知识库。
[Image]
2. Plan：Claim（AI生成的） + Insight(用户输入或者AI提示) → Protocol
 用户选择一个 Claim，输入 Insight（必须带 due_date，这个想法需要在确定的时间验证）。系统生成 Protocol.md（带具体实验的步骤与检查点），并准备 RunResult 模板（用于 Close闭环实验流程，给出反馈）。
[Image]
[Image]

3. Close闭环流程：RunResult(实际上是raw_data_paths，用户上传的各类实验数据，比如实验数据的csv文件) → Verdict + Writing Snippets
 用户填入结果摘要并提供 raw_data_paths（原始数据路径清单）。系统输出 Verdict（support/not-support/uncertain）与写作草稿（Methods/Results），并把它们回链到 Claim/Protocol。
[Image]
2.3 关键场景用例（Demo 内置 mock 示例）
Demo 内置多学科 mock 数据，用于展示“同一套六类对象与三条流线如何跨学科复用”。每个用例建议配一个短视频，强调“切换 Concept 后链路仍成立”。
- 用例 A：生物（Concept：代谢开关能否改变炎症表型）
 Ingest 提炼带证据摘录的 Claim；Plan 生成带检查点的 Protocol；Close 记录 raw_data_paths 并输出 Verdict 与 Methods/Results 草稿。
- 用例 B：化学催化（Concept：酸碱窗口是否决定活性/选择性）
 Claim 示例围绕条件窗口与关键变量组织；Protocol 将关键变量写成步骤检查点；Close 固化 RunResult 并生成可写入报告的结果段落。
- 用例 C：材料（Concept：稳定性阈值是否满足应用需求）
 Claim 聚焦稳定性阈值与性能保持；Close 阶段通过 raw_data_paths 保证可反查原始文件夹，并输出围绕稳定性的写作草稿。

This content is only supported in a Feishu Docs
项目 Demo 演示视频

---
三、技术实现路径（Obsidian 与产品共同管理同一套原子笔记）
[Image]
3.1 核心架构：本地优先的双端共生（同一份 Vault、双向可编辑）
本项目采用 Web 前端 + 本地 FastAPI 后端 + 本地 Markdown Vault 的本地优先架构。核心原则是：产品与 Obsidian 共同管理同一份 Vault 中的六类原子笔记资产，不复制、不搬运到云端数据库。
- Web 端（规范层）：通过 UI 表单与流程约束，推动用户按 Ingest/Plan/Close 完成科研动作，解决“执行力与规范性”。
- 本地端（资产层）：所有对象（Concept/Paper/Claim/Insight/Protocol/RunResult）均落盘为 Markdown 文件（含 YAML 元数据）。用户可在本地直接用 Obsidian/VS Code 打开、编辑、补充双链。
- 实现边界清晰：Web UI 不直接读写本地文件系统，而是通过本地 FastAPI 对同一 Vault 目录执行规范化读写，从而实现“UI 推进流程 + Obsidian 承载长期资产”的双端共生。
双端共生架构的核心优势
- 本地存储实验数据和原子笔记的Vault 是唯一真实数据源：后端每次响应与图谱生成均以磁盘上的 Markdown 文件为准，不依赖数据库。
- 本地手动编辑原始原子笔记会直接同步到 Web端产品：用户在 Obsidian 中补充正文、标签、双链，不影响系统后续读取与构图（核心依赖 frontmatter 的 id/type/关系字段）。
3.2 统一数据流与存储结构（六类对象 = 一套原子笔记，两端共用）
系统运行于用户的 /Research_Vault/ 目录下：
- Concepts/：Concept（今日验证对象）
- Papers/：Paper（文献元数据）
- Claims/：Claim（可证伪结论，必须含 evidence，即对应的 Papers 中的某一段落）
- Insights/：Insight（验证想法，必须含 due_date，即截止时间）
- Protocols/：Protocol（SOP）
- Runs/：RunResult（执行与结果，必须含 raw_data_paths；包含 Verdict 与写作草稿）

## 你需要做的
深度集合 json 文件中的实验周期应用场景，思考Researchloop-OpenClaw的记忆系统应该如何设计，
既要涉及有分时间阶段粗粒度+细粒度到每天的（工作记录、实验 sop），
也要有跨周期长期记忆的原子笔记系统

## yyq生成workspace的提示词

我想设计一个模仿 openclaw 的 贯穿一个环境学生三个月学习周期的Researchloop-OpenClaw

## 我的目标
以下是我想学习的 openclaw 的设计原则
- 文件即记忆 (File-first Memory)：摒弃不透明的向量数据库，回归最原始、最通用的 Markdown/JSON 文件系统。用户的每一次对话、Agent 的每一次反思，都以人类可读的文件形式存在。—对应我的原子笔记系统
-	技能即插件 (Skills as Plugins)：遵循 Anthropic 的 Agent Skills 范式，通过文件夹结构管理能力，实现“拖入即用”的技能扩展。
- 透明可控：所有的 System Prompt 拼接逻辑、工具调用过程、记忆读写操作对开发者完全透明，拒绝“黑盒”Agent。
由此可以得到
在一个完整的180 天的实验周期中（如 json 所示），用户可以清晰的看到workspace（前端）中Researchloop-OpenClaw是如何拼接上下文（用户问题+agents.md、soul.md、user.md、+tools 和 skill 清单+专属于实验学科定制的记忆系统（如下原子笔记系统）），并引导用户输入相应的实验结果/表征结果/相关的论文，然后清晰地在前端的中间的 chatbot 中看见 agent 的思考过程（其中推理过程+每一步引用了哪个 md 文件+信息不足时需要用户提供什么 context），并将最后的交付结果存档。如果检测到高度重复的存档，则可以沉淀为有用户个人风格的新的 tools 和 skills
初步前端设想（你暂时不需要考虑）：中间是 chatbot 的形式，左边是 layer1 和 layer2 的展示

## 我的产品的用户的记忆压力源+Researchloop-OpenClaw应该记住的东西

阶段性推进（每隔一段时间就要做组会/PPT、复盘卡点、列下一步计划）
你在周期中多次出现“第 N 次阶段汇报”的请求，且会带素材打包路径（如 assets/ppt_pack/R06_20251123，后面还有 R09/R10 等）。

实验闭环压力（今天做了什么，是否能支撑主线，要补什么对照/表征）
典型就是：合成 checklist、参数矩阵、以及“这组 XRD/SEM/XPS 能证明什么不能证明什么”。

机理证据链压力（你反复强调要补齐 Co(IV) / ClO₂ 的“硬证据链”）
你反复在 Close/机理环节追问 “PMSO探针 + DPD显色 + 淬灭剂 + 必要空白/对照” 的组合与判据。

写作结构压力（Results & Discussion 目录树 + 每节中心句 + 主文/SI放图策略）
你多次要“章节逻辑”树形目录，并且强调“每节要证明什么”。

所以你的记忆系统必须能同时做到：

粗粒度：阶段里程碑、主线假设、当前最大不确定性、下一步验证计划

细粒度：每天实验 SOP、编号/污染风险提醒、数据路径、拟合结果、图表产物

跨周期：可追溯的 Claim-evidence、可复用的 Protocol、可累计的 RunResult 反向索引

可沉淀：高重复的交付（比如“按时间顺序 checklist”、“阶段汇报PPT提示词”）自动提炼成 Skill


## 你的参考
- json文件
json 文件是我模仿用户在workspace 前端中整个周期的提问和输入。
- 一个ZIP文件openclaw_templates.zip
里面可能有各种对于这个需要组装的project context的初始化内容，尤其是我的记忆系统中的 md 文件中的 layer1。**这里很有可能需要有不同的文件夹，比如 claims/claim.md,因为这个还可以是不同的路径**
- 记忆系统设计 md 文件
你需要基于这个文件中的内容，将 layer1 、layer2 的文件需要学习到 openclaw 的精髓，layer3 则是需要深度结合 json 中用户昌吉

## 你的输出
我在附件有一个openclaw_pi_prompt_concat_concise.md, 你需要交付一个PRD文档prompt_prd.md，来告诉模型如何使用这些上下文记忆的管理模式，以及如何在实际的研究中应用这些模式。我会使用这个PRD文档进行agent的开发。这个PRD文档要清楚的说出上下文是如何拼接的，还有一些固定的提示词应该如何写。**并且也给我举出一个例子来，在prompt_prd.md里面**。
我在附件有一个ZIP文件openclaw_templates.zip，里面有不少的XXX.md文件，你也需要给我一个ZIP文件叫做experiment-research-openclaw-templates.zip，里面可能有各种对于这个需要组装的project context的初始化内容。experiment-research-openclaw-templates.zip 里面有很多的markdowns，这个是要作为一个工作区的初始化文档的。**这里很有可能需要有不同的文件夹，比如 claims/claim.md,因为这个还可以是不同的路径**

# 2026-2-27
https://mp.weixin.qq.com/s/PRLrVURRycKOhnFOGGdJYA
这个真的说的很好，就是一个是openclaw这种实践的模式，一个是快速迭代的能力，就是快速开始，然后迭代。
## 发现一个问题，就是PRD和TAD文档不好联合修改，以及给出上下文， 这个只有靠codex硬写
这里还有一个问题，是否跨会话的长期记忆是如何做的。
https://docs.openclaw.ai/zh-CN/concepts/memory
可以查看下这里的文档，就是有两种记忆，一种是常规的精选记忆，一种是日常记忆。
主要需要思考下工作区的隔离问题，就是比如开一篇新的文章的问题。
## 发现GPT5PRO写的 PRD 和TAD文档幻觉严重，基本不能用，前后严重不一致，可能需要cc的plan计划然后最后 能够保证前后的一致性。
## 感觉需要头脑风暴plan模式，然后疯狂的迭代，最后才能得到一个比较完善的PRD文档和TAD文档。
## 有了之后立马进行快速的实操和功能的迭代

# 2026-3-2
现在是session_id和这个workspace_id没有对应的太好
# 2026-3-3
很明显的PRD和TAD的关于某一个的内容不一致，尤其是最核心的api call的不一致，这里明显需要开发过的人才知道这件事如何做。
发现改了很多很奇怪的东西，如果没编程过，完全不知道在说什么。比如这个SSE的event是不用这个json里面带着是type的

归档和压缩功能感觉
这里好像也没有说清楚资料上传了之后放哪里，就是每个比如调用这个API 之后 放在哪里的情况.
会发现这个workspace_id 其实是agent_id 这两个是同一个东西

发现trace 这么多的字段是凭空生成的啊,我挺无语的. 这里的问题是这个session_json在不压缩的情况下其实就是连续对话,那么连续对话就没有太多的问题.

memory pack这些所有东西全部删掉,因为这里落盘这些应该是主动交给Agent去做的，而不是我们的代码里面有一步单独这样做.

#  2026-3-20
现在我要在auto-eval里面写代码，你参考
backend\auto_eval\eval_system_design_prd.md
backend\auto_eval\eval_system_design_tad.md
实现在backend\auto_eval路径下面，尽量是完全和后端解耦的，测试的时候需要调用后端的FASTAPI的API接口，
backend\auto_eval\{B/C/D/E}_questions_and_eval_methods下面的评测代码仅供参考，可以优先使用，但是你实际发现有问题的时候可以修改。
在书写测试的时候一定要多命令行输出，确保能够清晰地看到每一步的执行过程和结果。因为整个测试过程大模型的调用可能会花很多时候，最后也要写入一个md文件最后该次测试的总分数。

## 终端工具适配
terminal tool 有问题，这里我也可能会有mac电脑运行的情况，你需要设计一个检测的命令，就是并且在发送给AI的提示词里面提示是MAC系统还是Windows系统，这个软件设计也挺重要的。
backend\graph\prompt_builder.py
改在这个文件里
明确提示模型优先用 Windows 命令：dir / where / type / findstr，MAC下面的 ls / find / sed，对于这些指令也有在不同系统不同的提示状态

## 短暂的skill工作区文件书写
现在有规定短暂的一些py文件或者json文件写在哪里吗，这些往往是一些短暂的py执行文件，或者管道的输出？

你在workspace-templates里面增加一个temporary_dir 并且在 Agents.md 里面指定这个是放短暂文件的地方，包括不限于暂的py执行文件，或者管道的输出，等等，并且增加代码，每次session 清空的时候会清空。
## 结果报告写在哪里
现在workspace-template有规定这个如果有输出 docs或者ppt放在哪里吗？

## context_trace
我观察到context_trace里面有两种json，一种T000X.json，一种是UUID.json,这两种有什么区别吗？
### 这个可能需要重新设计
为啥AI写落盘的json，以及自动化也写落盘的json

## eval进度条
eval的时候增加进度条，能够让用户看到现在测试完成了百分之几了。

## git提交整理
现在git有海量的提交，你先把里面的auto_eval的拿出来提交了

## eval的log file
尽量在命令行和log中不要输出内容只用输出部分的关键信息，不要输出内容（内容往往有换行，直接就覆盖掉很多信息了），不然会污染命令行，然后相关的log信息写到 backend\auto_eval\logs，
算了，这个log路径放在eval_runs的对应文件夹下面方便点，能看出来哪个是哪个实验的

## 重新设计bootstrap流程
现在我要把之前分类型这些全部删掉，现在不需要分类型，分意图这里没有用，这段代码先删掉。这里需要bootstrap在最开始有一个提问给用户，然后用户需要简短回答，
参考openclaw做（openclaw的方法如下）：
  onboarding/setup
  → 创建/确认 workspace
  → 写入默认模板文件
  → 仅在“全新 workspace”时写入 BOOTSTRAP.md
  → agent 启动时加载 bootstrap 文件集合
  → 过滤/裁剪/扩展这些文件后注入 prompt
  → agent 按 BOOTSTRAP.md 做首次问答
  → 更新 IDENTITY.md / USER.md / SOUL.md
  → 删除 BOOTSTRAP.md，后续就不再跑首次 bootstrap。
这里你的提问的提示词直接固定一个，固定在Bootstrap.md里面，然后程序运行的时候读取，然后把整个Bootstrap.md作为引导的提示词，
所以我再给你发送下询问的流程
1、从bootstrap.md里面读取提示词，提示词的内容是这样的（你设计一下，针对当前的化学材料实验相关的，）
2、前端显示这个问题，发送过去（用户在这里也可能上传文件啊，所以这里是正常相当于用户的提问，复用chat这个api接口）
3、在Bootstrap.md里面记录用户的回答，这里md应该专门有一个预留位置写用户的第一次简短QA的结果，然后就进入正常的对话核心调用（这里复用的之前的对话函数，相当于全部的md都作为提示词，要求更新 IDENTITY.md / USER.md / SOUL.md），
4、删除 BOOTSTRAP.md，后续就不再跑首次 bootstrap。
这里为了复用之前的对话函数

现在要对 backend\auto_eval\{B/C/D/E}_questions_and_eval_methods 进行相应的调整,因为现在有bootstrap的，逻辑，所以最开始用户要先描述下这里的实验目标和背景信息，然后再进入具体的问题和评测方法的讨论。 这样就不会出现backend\auto_eval\analysis_result_and_badcase这里的问题。