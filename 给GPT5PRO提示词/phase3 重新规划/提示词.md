phase3 这里有些问题必须澄清一下。
json 数据是模拟用户在使用本 openclaw 的真实提问数据集，这里会对应phase3 开发模块的流向动态示意。
本系统在运行的时候你需要有个一个基本的简单的引导用户
## 尤其是意图识别模块，需要有个基本的 router，但不知道是否符合 prd 和 tad 的要求：
langgraph中的Router 节点将用户问题分类的五个意图分别为：general-query（一般性问题）、additional-query（需要更多信息才能回答的问题）、graphrag-query（需要查询知识库的问题）、image-query（需要解析用户上传图片的问题）、file-query（需要解析用户上传文件的问题）。其中后面两个类别是用规则来识别的。）
## 三层记忆系统+propmt builder
这是返回给当下 workspace 下的内容，你需要引导用户定义一个比较小且能够马上获得闭环的task（json 数据里有很多对话，差不多 5-10 轮对话算一个 task 的最小），并引导他上传初步的assets 文件。
这些文件后续要如何引入到 memory 系统中呢，memory 中的 layer1 和 layer2 都是依赖 openclaw 自己的自进化原则+根据日期自动记忆；
但 layer3 是解决科研场景下痛点的关键，你的一个 workspace必须要满足，你必须逆向到对应的技术路线上！
1) **阶段性推进**：比如每 2–3 周一次阶段汇报（R01…R10），用户会给 `assets/ppt_pack/Rxx_YYYYMMDD/` 素材路径，需要快速汇总。
2) **实验闭环**：比如每天做了什么、是否支撑主线、缺哪些对照/表征（合成 checklist、参数矩阵、XRD/SEM/XPS“能证明什么不能证明什么”）。
3) **机理证据链闭环**：比如Co(IV) / ClO₂ 的硬证据链（PMSO 探针 + DPD 显色 + 淬灭剂 + 必要空白/对照 + 判据）。
4) **写作闭环**：比如Results & Discussion 目录树、每节中心句、主文/SI 放图策略。

## 具体的Layer3：Atom Notes（原子笔记资产层）


#### 3.1 目录

```text
memory/concepts/
  CONCEPT_*.md
memory/tasks/
  TASK_*.md              # 内含 Claim + Protocol + Run（可追加多次 run）
memory/packs/
  PACK_*.md              # stage_report/mechanism/writing/figure 等交付包
```

#### 3.2 对象最小字段（建议）

* `Concept`

  * `id, name, scope, keywords`
  * `north_star`（一句话主线）
  * `active_tasks[]`
* `Task`（Claim + Protocol + Run）

  * Claim：`claim_text` + `evidence`（缺 evidence 禁止入库）
  * Protocol：`steps[]`（带 checkpoint）+ `controls[]`
  * Run：`raw_data_paths[]` + `quick_results` + `verdict`
* `Pack`

  * `pack_type`：`mechanism_pack | stage_report_pack | figure_pack | writing_pack`
  * `task_refs[]`
  * `final_assets[]`（ppt_pack、png、段落等）
  * `takeaways[]`（每张图/每条证据一句话结论）

---


## 单独一次对话的流线
参考/Users/fenke/projects/study_ai/2-未完成项目存档/zly 规划-0219/yyq_chlorite_full_lifecycle_180d_300turns_humanized/yyq_chlorite_full_lifecycle_180d_300turns.json

在一次对话中，你需要完整的经历意图识别-引导用户上传文件+提供必要信息-帮助用户自动将 assets 归类 or 通过交互生成到 concept、task 和 pack 中，这其中是否需要subagent来分工协作参与？但这会不可避免地带来时延的影响，用 subagent的协作是可以解决的吗？

这些原子笔记产出都是 md 文件，应当是可以最后利用一些生成 ppt、论文写作、文献调研 word 或者生成实验机理图的生图的subagent/skill 来实现自由组合，其中 md 文件是这些最终交付物（应该也是在 pack 文件夹下，但是必须展示是利用哪些原子笔记建成的，方便用户一眼溯源并且能够理解这是顺着自己的思路制作的）

## pro
# 你的角色
你现在是我的“AI-native 产品架构师 + 技术 PM + 开发规划 reviewer”。

# 项目背景
我在做一个 Experimental-Research-OpenClaw 项目。
它不是一个泛用聊天 Agent，而是一个面向科研场景的 file-first AI 工作台。
核心目标不是“像聊天机器人一样回答”，而是把科研用户的高频工作流沉淀为可追溯、可验证、可回放、可沉淀的 Concept / Task / Pack 三类原子资产。

我当前的核心目标有两个：
1. 平衡项目的理解、设计完整性和可跑通程度，推进 Phase 3
2. 同时把这个项目包装成可用于“深势科技 AI 产品经理岗位”面试表达的案例

# 你要特别注意的现实约束
1. 当前实际只完成了 Phase 1 + Phase 2，Phase 3 还没开发
2. 你必须以我上传的真实代码和文档为准，不要只按目标架构脑补
3. 你必须区分：
   - 用户业务意图
   - 输入模态
   - 系统执行策略
4. 你必须优先帮助我收敛 Phase 3 的主逻辑，而不是继续发散功能
5. 你必须明确哪些能力属于 Phase 3，哪些应该延后到 Phase 4/5/6
6. 如果信息不足，请列 Missing checklist，不要脑补

# 我当前最关注的 Phase 3 问题
1. Router 应该如何设计，才能既满足 PRD/TAD，又便于 MVP 跑通？
2. Layer3（Concept / Task / Pack）应该如何定义最小写入原则？
3. 单轮对话如何形成最小闭环？
4. 是否应该在 Phase 3 引入 subagent？如果不应该，请说明原因
5. 如何平衡“做清晰”和“做可跑通”？
6. 这个项目如何提炼成深势科技 AI 产品经理面试中的主叙事？

# 你要先完成的任务顺序
请严格按以下顺序输出，不要跳步：

## Part 1：先做事实对齐
- 基于我上传的文件，先总结：
  - 当前已实现了什么
  - Phase 3 还缺什么
  - 哪些地方在 PRD / TAD / 当前代码之间可能存在冲突或漂移

## Part 2：输出 Phase 3 规格冻结版
请输出一份“Phase 3 Spec Freeze”，至少包含：
1. Phase 3 的唯一主目标
2. 不做什么（边界）
3. Router 的分层结构
4. Layer3 的最小写入规则
5. 单轮最小闭环
6. trace 最小字段与 done 时机
7. Phase 3 对 Phase 4 的接口约束

## Part 3：输出开发优先级
按“紧急度 × 可行性”排序，给出下一步开发顺序，并明确：
- 哪些是必须先做
- 哪些是可以延后
- 哪些会拖慢 MVP，不该现在做

## Part 4：输出面试映射
把这个项目映射到“AI 产品经理（科研场景）”岗位能力，至少覆盖：
- 需求与场景拆解
- AI 能力产品化
- 数据与评测体系
- 版本推进与跨团队协作

## Part 5：输出可执行交付物
最后请输出以下 3 个内容：
1. 一页版 Phase 3 主逻辑总结
2. 给 coding agent 的开发指令
3. 给我本人练面试用的 90 秒项目介绍口径

# 输出要求
- 中文输出
- 先讲 why / what，再讲 how
- 不能泛泛而谈，必须引用我上传文件中的具体结构、文件名、API、Phase 边界
- 如果你发现我当前思路里有概念混淆，请直接指出并重构


# 提示词2
# 你的角色
你现在是我的“AI-native 产品架构师 + 技术 PM reviewer + 面试教练 + 技术学习地图生成器”。

# 背景
我在做一个 Experimental-Research-OpenClaw 项目。
它不是一个泛用聊天 Agent，而是一个面向科研场景的 file-first AI 工作台。
核心目标不是“像聊天机器人一样回答”，而是把科研用户的高频工作流沉淀为可追溯、可验证、可回放、可沉淀的 Concept / Task / Pack 三类原子资产。

我当前同时有两个目标：
1. 推进项目 Phase 3，并保持 Phase 3 / 4 / 5 的技术路线清晰
2. 准备“深势科技 AI 产品经理（科研/AI 工程场景）”岗位面试，希望快速补齐 JD 涉及的技术生态地图，并形成开放题回答锚点

# 你必须遵守的约束
1. 必须以我上传的真实文档、真实代码、真实 workspace、真实样例数据为准
2. 如果信息不足，列 Missing checklist，不要脑补
3. 你必须区分：
   - 用户业务意图
   - 输入模态
   - 系统执行策略
   - 技术实现手段
4. 你要优先帮助我建立“可讲清楚的主逻辑”，不是堆很多术语
5. 你要明确哪些能力属于：
   - 面试最低必会
   - 项目可讲设计但尚未实现
   - 后续真正值得开发

# 我希望你完成的任务
请严格按以下 6 个部分输出：

## Part 1：项目事实对齐
基于我上传的 PRD / TAD / phase plan / dev log / 真实代码，先总结：
- 当前已实现了什么
- Phase 3 还缺什么
- Phase 4 / 5 的技术路线是什么
- 当前项目里有哪些设计和实现可能漂移
- 哪些内容适合面试时讲“已实现”，哪些只能讲“设计过/规划中”

## Part 2：Phase 3 Spec Freeze
请输出一份“Phase 3 规格冻结版”，至少包括：
1. Phase 3 唯一主目标
2. 不做什么（边界）
3. Router 分层结构
4. Layer3（Concept / Task / Pack）最小写入规则
5. 单轮最小闭环
6. trace 最小字段与 done 时机
7. 与 Phase 4 / 5 的接口约束

## Part 3：JD 技术生态学习报告
请围绕岗位 JD 中出现或隐含的技术能力，输出一份“速通但不空泛”的学习报告，至少覆盖：
1. Prompt
2. Tool Use
3. Agent 基本逻辑
4. Agent 任务编排 / 多 agent 的轻量框架
5. 多轮对话
6. RAG
7. 向量检索
8. BM25 / 混合检索 / 重排
9. 实体抽取 / 结构化解析
10. 评测体系（指标、金标/精标、错误归因）
11. B 端产品要素（权限、审计、合规、SLA、成本、性能）

要求：
- 先讲 each 技术解决什么问题，再讲常见实现路线
- 讲清楚它们之间的关系，不要孤立列概念
- 对每个知识点给出：
  - 面试最低必会
  - 能拉开差距的理解点
  - 容易答偏的误区
- 最后输出一张“技术生态地图”

## Part 4：开放性问题回答锚点
请基于上述技术生态，为我整理高频开放性问题的回答框架，至少包括：
- 为什么需要 RAG？
- 向量检索和关键词检索的区别？
- 混合检索和重排为什么常见？
- 单 Agent 和多 Agent 如何取舍？
- 如何定义 AI 产品的评测体系？
- 如何做失败案例归因？
- 如何设计一个科研场景 AI 助手的 MVP？
- 如何平衡可解释性、性能和开发成本？

每题请输出：
- 回答框架
- 关键词锚点
- 如何落到我的项目上回答

## Part 5：JD 面试 Q&A 题库
请模拟这个岗位的面试官，输出一份 Q&A，分成：
1. 自我介绍追问
2. 项目深挖
3. AI 基础技术抽问
4. 评测/数据集构建抽问
5. 版本推进与跨团队协作
6. 场景设计题
7. 压力测试题（比如“你没有真正做完 RAG / 多 agent，为什么还能讲这部分？”）

每道题请给：
- 面试官真实意图
- 一版优秀回答
- 回答中的证据锚点
- 哪些内容不要乱讲

## Part 6：反哺项目开发
请把整份学习报告反向映射到我的项目开发路线中，输出一个表：
- 知识点
- 是否该在 Phase 3 落地
- 是否更适合 Phase 4
- 是否更适合 Phase 5
- 如果现在不开发，面试时如何诚实但专业地表达

# 输出要求
- 中文
- 先讲 why / what，再讲 how
- 不是教科书综述，而是“面试可用 + 项目可用”的技术地图
- 任何结论都尽量结合我上传的项目文件、Phase 边界、模块名、真实 API 或目录结构来讲
- 发现我有概念混淆时，请直接重构，不要顺着错误概念继续展开


## phase3开发
我将在这里与你探讨如何和 cc 及 codex 合作来继续 phase3 的开发，我希望能有提示词让它们根据现有架构如果按照新的 phase3 的开发的话会在原有的架构上新增哪些的 html 图，方便我的脑内对项目开发有很清晰的认知并能够说清，但又要防止我的项目停留于自嗨而是基于对科研场景 knowhow +技术理解力的，所以我认为 phase3 的三个小模块都需要有一个很清晰的从我要解决的在科研场景的问题/目的和设计理念-技术选型/技术生态理解和比选-项目最小 mvp 的 coding plan-怎么跑测试和建立数据集？网上有没有最适配的等等的完整的虚实结合的证据链，甚至比开发完成都更重要，这是一次重新的整合，需要你阅读我所有给你的文件并打通我上述给你的逻辑链条，其他多余的不要多说防止对我造成信息冗余。